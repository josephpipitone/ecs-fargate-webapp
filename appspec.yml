version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: hello-world-app
        LoadBalancerInfo:
          ContainerName: "hello-world-container"
          ContainerPort: 80